#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
PYTHON=${PYTHON:-python3}

# Ensure src is on PYTHONPATH so module SOF0273 can be imported with "python -m SOF0273"
export PYTHONPATH="$ROOT_DIR/src:${PYTHONPATH:-}"

exec "$PYTHON" -m SOF0273 "$@"
