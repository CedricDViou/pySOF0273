[pytest]
# Ensure pytest only looks in the repository `tests/` directory for test collection
testpaths = tests
python_files = test_*.py

# Prevent pytest from importing modules from outside the project root
# (helps avoid "import file mismatch" when other copies of the tests exist on sys.path)
addopts = -q
